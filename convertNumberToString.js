// We need a function that can transform a number (integer) into a string.

//give an interger of any length, always an integer
//return the integer as a string

function numToStr (num) {
    return num.toString()
}

// console.log(numToStr(123), "123")
// console.log(numToStr(-65), "-65")
// console.log(numToStr(0), "0")
